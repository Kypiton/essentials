name: Essentials Workflow
on:
  workflow_dispatch:
    
jobs:
  prints:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello GH Actions!
      - name: Print some lines
        run: |
          echo First Line!
          echo Second Line!
